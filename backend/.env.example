# Server port (default: 3001)
PORT=3001

# API authentication token — set to a secure random value to enable auth.
# When empty, authentication is disabled.
# Clients must send this token in the X-API-Token header.
API_TOKEN=

# CORS origin — the URL of the frontend app (default: http://localhost:5173)
CORS_ORIGIN=http://localhost:5173
